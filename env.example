# FAA 环境变量配置
# 复制此文件为 .env 并填入实际值

# ========== 必需配置 ==========

# LLM 提供商（二选一或按需切换）
# 1) OpenAI 兼容（默认，亦可用于 Qwen/Moonshot/DeepSeek/Groq 等 OpenAI 兼容端点）
AI_PROVIDER=openai_compatible
OPENAI_API_KEY=sk-UbGkAyjIojKwdPoNAAAR5lgl3sBw72RUthueZxfuKodzEtjC
# 若使用国内 OpenAI 兼容端点，请设置 base_url，例如：
# Qwen（通义千问 DashScope 兼容）：
# OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# Moonshot（Kimi）：
OPENAI_BASE_URL=https://api.moonshot.cn/v1
# Groq：
# OPENAI_BASE_URL=https://api.groq.com/openai/v1
# DeepSeek：
# OPENAI_BASE_URL=https://api.deepseek.com/v1

OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
EMBED_PROVIDER=local_fastembed
FASTEMBED_MODEL=BAAI/bge-small-zh-v1.5

# 数据库密码（请使用强密码）
POSTGRES_PASSWORD=your_strong_password_here

# 应用密钥（请生成随机字符串）
SECRET_KEY=your_random_secret_key_here

# ========== 可选配置 ==========

# Threema Gateway（如需接收消息）
THREEMA_GATEWAY_ID=*XXXXXXX
THREEMA_SECRET=your_threema_secret
THREEMA_PRIVATE_KEY=
THREEMA_WEBHOOK_URL=

# 高级配置（通常不需要修改）
LOG_LEVEL=INFO
APP_ENV=production
MCP_SERVER_URL=http://localhost:9000

# 2) Anthropic（Claude）（可选）
# AI_PROVIDER=anthropic
# ANTHROPIC_API_KEY=sk-ant-xxx
# ANTHROPIC_MODEL=claude-3-7-sonnet-latest

# ========== 媒体与多模态 ==========
MEDIA_ROOT=/data/media
MEDIA_PUBLIC_BASE_URL=
SIGNING_SECRET=
ENABLE_STT=true
ENABLE_OCR=false
ENABLE_VISION=false